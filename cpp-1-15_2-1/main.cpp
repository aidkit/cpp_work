#include <iostream>

struct Foo 
{
    int         mInt;
    unsigned    mUnsigned;

    Foo(int iInt, unsigned iUnsigned):
        mInt(iInt),
        mUnsigned(iUnsigned)
        {}
};

int main(int argc, char const *argv[])
{
    // Foo and pointer
    return 0;
}
